<p>
<h5>Comments</h5>
</p>
<p>
  <%= comment.content %> <small> -<%= comment.user.name %></small> &emsp;
  <% if(locals.user && locals.user.id == comment.user.id) { %>
  <button><a href="/comment/destroy/<%=comment.id %>">delete comment</a></button>
  <%}%>

  <span>
    <small>
      <% if(locals.user){ %>
        <button><a class="toggle-like-button" data-likes="<%= comment.likes.length %> "  href="/likes/toggle/?id=<%=comment._id%>&type=Comment">
          <%= comment.likes.length %> Likes 
        </a></button>
      <% }else{%>
        <%= comment.likes.length %> Likes
      <% } %>   
    </small>
  </span>
  
</p>
